info:
  name: deletebooksid
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{x}}/books/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
