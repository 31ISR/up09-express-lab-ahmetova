info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{x}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "qwerty52@gmail.com",
        "password": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
