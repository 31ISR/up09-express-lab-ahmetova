info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{x}}/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "qwerty123@gmail.com",
        "username": "qwerty123",
        "password": "qwerty123",
        "role": "user"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
